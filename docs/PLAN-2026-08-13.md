# Plan vigente (rama `core/esqueleto`)

> Reescrito el **2026-08-12** tras una revisión completa con Beltrán: se auditó guion vs. plan vs. lo construido, y se cerraron **11 decisiones** por preguntas directas. Esto reemplaza al plan anterior de este archivo. Mapa de Blueprints: [`.claude/skills/unreal-vr/blueprints/_INDEX.md`](../.claude/skills/unreal-vr/blueprints/_INDEX.md).

---

## 0. 🔴 Las 11 decisiones cerradas (2026-08-12) — ninguna se re-discute sin Beltrán

| # | Tema | Decisión |
|---|---|---|
| 1 | **Recentrado** | **SÍ**: `ResetOrientationAndPosition` en BeginPlay. Todo lo espacial se ancla al **mundo** y se autora con **TargetPoints**. Muere el sistema camera-relative del panel del título → panel y botones pasan a ser relativos a un único `MenuRoot` junto al `PlayerStart`. Resuelve de raíz el bug de los 5,45 m. |
| 2 | **Salas** | **Cada sala es SU PROPIO `.umap`**, con meshes y materiales que diseña Beltrán: **6 mapas** (Hall, Entering, Recognizing, Loving, Attracting, Surrounding — el final NO es una sala, ver #7). El director migra de `RoomMap` único a un **array `RoomMaps`**. Migrar **ya**, con copias del placeholder, para que nada nuevo asuma "un solo mapa". |
| 3 | **Orden** | El §10 del doc maestro **sigue firme**: esqueleto completo y caminable antes que mecánicas. **`BP_SoulChoice` queda CONGELADO** hasta el paso 4 (es contenido del Hall). |
| 4 | **Puertas** | **CERRADA hasta el final**: se camina hacia la puerta cerrada (cartel encendido, resplandor por la rendija) y **abre recién al llegar**, con el negro cayendo. Rehace la secuencia actual (hoy abre al empezar a caminar). Supersede el "bug #3" del director. |
| 5 | **Intro** | **La voz suena MIENTRAS se camina** (escenas 1 y 2 juntas, un solo bloque de ~45-50 s donde también se toma el baseline). Para testear: **placeholder corto**; cuando exista la grabación real se ajusta el tempo. 🔴 Todos los tiempos del corredor como **variables**, nunca hardcodeados. |
| 6 | **Recognizing** | La subida es una **ilusión**: **el pawn NO se mueve — el entorno desciende** (columna de luz / anillos bajan alrededor). No es un walker hermano: es un BP de esa sala. La sala no necesita altura real. |
| 7 | **El final** | **NO hay última puerta ni avance.** Al completarse la carga de Surrounding, el pawn se queda en su punto y **la arquitectura se deshace en el lugar** (animación tipo *transformer*) → se está en el **exterior** (`BP_Void`): gráfico, pregunta de Alma, elección, constelación, despedida. `FINAL` sale de `StageNames`. |
| 8 | **About Us** | Panel de **texto + botón BACK** que vuelve al menú, mismo lenguaje visual del título. Placeholder hasta que Beltrán pase el contenido (en inglés). |
| 9 | **Timbre** | Se construye **junto con el corredor** de la intro: menú → voz+caminata → timbre (apoyar la mano y esperar) → Hall. Cierra la intro de punta a punta. |
| 10 | **Alma ≠ Proto Soul** | **Dos actores distintos.** Alma es la guía (Hall y final; futuro `BP_Alma`, paso 4); el Proto Soul es el HUD personal del usuario. No fusionarlos ni compartir instancia. |
| 11 | **Optimización** | Ratificada la regla §9.1.b: **spawn → uso → kill**, posición por TargetPoint, memoria al mínimo (VR standalone). Violaciones pendientes de corregir: el panel del título y las 3 amebas candidatas viven colocados en el nivel. |

---

## 1. 🔴 Protocolo de "terminado" — endurecido tras la revisión

**Nada se le entrega a Beltrán para revisar sin pasar TODO esto.** El diagnóstico de la revisión fue claro: el plan estaba bien; lo que falló fue que el testeo veía lógica (log) pero no **espacio** ni **imagen**.

1. Compilar + Save All.
2. Batería `BP_SelfTest` verde en **PIE y en Simulate** + barrido `GetLogEntries(pattern:"Accessed None")` — no omitir.
3. 🆕 **Aserciones ESPACIALES**: verificar posiciones **relativas al pawn/cámara**, no existencia. "El botón se spawneó" no dice nada; "el botón está a <1 m del forward de la cámara" habría atrapado el bug de los 5,45 m sin visor.
4. 🆕 **Captura obligatoria**: `CaptureViewport` en el beat en cuestión → volcar a PNG → **mirarlo de verdad**. Deja de ser recurso de emergencia y pasa a ser el último paso de rutina.
5. Variables instance-editable: `get_properties` sobre la **INSTANCIA** (la regla de las 7 mordidas, ver §3) — y **generalizar ya** la aserción de ceros en `BP_SelfTest` en vez de sumar la octava a mano.

🔴 **Lo que se MIDE va por log; lo que se SIENTE, sólo con el visor puesto.**

---

## 2. Orden de trabajo (reemplaza la lista anterior)

> 🟢 **ESTADO 2026-08-12 (tarde): tareas 1-8 EJECUTADAS salvo partículas y HUD debug.** La maqueta corre **de punta a punta por log**: intro → menú (MenuRoot único, aserciones espaciales verdes) → corredor por el vacío → timbre del Center (spawn/kill) → 6 salas con sus 6 `.umap` propios → puerta cerrada hasta llegar → disolución final al exterior. Arnés: **30 pass / 2 fail correctos / 0 skip**, cero `Accessed None`. Andamiaje de prueba sin visor: `bAutoStartAfterTitle` (intro) + `bAutoRing` (director), ambos en **false** en las instancias. **Pendiente próximo:** partículas del exterior (leer `niagara-quest.md` antes) · HUD de debug §9.9 · y el **test en visor de todo** (comodidad del corredor, timbre con la mano, About US, puerta al llegar).

| # | Tarea | Nota |
|---|---|---|
| 1 | **Recentrado en BeginPlay + `MenuRoot` único** | Decisiones #1. Un solo TargetPoint `MenuRoot` junto al `PlayerStart` (~(−500, 0, 120)); panel y botones relativos a él con los offsets que ya existen (`PanelDistance`, `ButtonDistance`, `ButtonSpread`, `ButtonDrop`). Elimina la mezcla de sistemas de referencia. |
| 2 | **Verificar el kill del menú** | Al apretar START debe salir `INTRO: menu destruido, cero residuos`. |
| 3 | **Migrar el director a `RoomMaps` (6 mapas)** | Decisión #2. Crear `L_Room_Hall`, `L_Room_Entering`, `L_Room_Recognizing`, `L_Room_Loving`, `L_Room_Attracting`, `L_Room_Surrounding` (copias del placeholder por ahora) y que `PreloadNext` indexe el array. `FINAL` sale de `StageNames`/`StageColors`/`RoomKinds`. |
| 4 | **Rework de la puerta: cerrada hasta llegar** | Decisión #4. La secuencia pasa a: dim → trazar marco + cartel → caminar hacia la puerta CERRADA → al llegar, abre + cae el negro → swap. ⚠ Revisar las restricciones de tiempos del tracker (`RevealHold`, `LegTime ≥ FadeOutTime`) que cambian con el nuevo orden. |
| 5 | **El caso terminal nuevo: el deshacerse** | Decisión #7. `FinishObra` = animación de disolución de la sala de Surrounding (a `BP_Room` le falta un `Dissolve()`/`OpenWalls()`) → queda visible `BP_Void` → placeholder de constelación. Sin puerta, sin caminata. |
| 6 | **El corredor de la intro + timbre + puerta del Center** | Decisiones #5 y #9. `BP_Walker.BuildPath(HalfLen)` ya existe. Voz placeholder corta con duración variable; el largo del corredor se deriva de la duración de la voz (baseline §5). Timbre: apoyar la mano + esperar (hold) — la mecánica de hold ya existe probada. |
| 7 | **Spawn/kill del panel del título y las amebas candidatas** | Decisión #11 — la regla §9.1.b vale para todo. |
| 8 | **El panel de ABOUT US** | Decisión #8. Texto placeholder + BACK. |
| 9 | **Las partículas del exterior** | El paralaje que da la amplitud. El MISMO sistema en la intro y en el final (firma del arco, §6.b). |
| 10 | **El HUD de debug del §9.9** | El doc maestro lo llama el mayor ahorro de tiempo del proyecto. |

**Congelado hasta el paso 4 del §10:** `BP_SoulChoice`, `BP_Alma`, contenido del Hall, mecánicas de etapas.

---

## 3. 🔴🔴 El patrón que ya mordió SIETE veces: instance-editable nace en 0
Cada variable instance-editable **nueva** aparece en **0 / false / array vacío** en los actores **ya colocados**, aunque la clase tenga otro default. Los siete casos: el bob del walker · `bIsHUD` de la ameba · `HallDuration`/`FinalDuration` del director · `ClearFadeTime`/`TitleFadeOut` de la intro · `ButtonDistance` · `MenuLabels`.

**Procedimiento obligatorio al agregar una variable instance-editable:**
1. Setear el default en el CDO.
2. **Setearla también en cada actor colocado.**
3. **Verificarla con `get_properties` sobre la INSTANCIA**, nunca sobre la clase.
4. 🆕 **Generalizar la aserción de ceros en `BP_SelfTest` AHORA** (decisión de la revisión) — no esperar la octava mordida.

---

## 4. Pendientes de Beltrán (insumos, no bloquean el esqueleto)
- **Guion / grabación de la voz** de la intro (~45 s) y las líneas de Alma. Mientras: placeholder corto.
- **Texto de ABOUT US** (en inglés).
- **Meshes y materiales de las variantes de Proto Soul** (hoy sólo cambian de color) — para el paso 4.
- **Diseño visual de cada sala** — cuando existan los 6 `.umap` (tarea 3), a su ritmo, sin tocar código.

---

## 5. Cómo verificar sin visor (el bucle que ya está armado)
```
StopPIE  →  compilar  →  StartPIE({bSimulate:false, warmupSeconds:16})
GetLogEntries(pattern:"TEST ",        category:"LogBlueprintUserMessages")
GetLogEntries(pattern:"Accessed None", category:"")     ← NO OMITIR
StopPIE
```
**Estado del arnés: 26 pass.** Los 2 fail son `sala visible en el mundo` y `hay al menos 1 sala`, y son **correctos**: con `bAutoStartAfterTitle` en false la sala no existe hasta que se apriete START. ⚠ El arnés todavía no distingue "no hay sala porque se rompió" de "no hay sala porque la obra no arrancó" — arreglarlo para no acostumbrarse a ver rojo.

🔴 **Y cuando algo no se ve: `CaptureViewport` + volcar el base64 a `VR_Test/Saved/` + decodificar y mirar el PNG.** (Ahora es parte del protocolo §1, no una emergencia.)

## 6. Trampas del MCP ya pagadas (no volver a pagarlas)
- 🔴 **`SceneTools.find_actors` MIENTE**: devolvió vacío para actores que `get_properties` leía sin problema. **No sirve como prueba de existencia.** La prueba que vale es desde el juego (`GetActorOfClass` en el arnés).
- 🔴 **Un `execute_tool_script` que falla ROLLBACKEA lo que hizo… pero no todo**: `add_expression` de materiales y `add_component` **sobreviven**. Después de un script fallido, **verificar qué quedó**.
- 🔴 **`try/except` no salva un script**: la excepción aborta la corrida igual. Lo que puede fallar va en su **propia llamada MCP**.
- 🔴 **No compilar con PIE corriendo** — colgó el editor una vez y hubo que matarlo.
- ⚠ **Al insertar un nodo "al principio" de un EventGraph, elegir el evento por su `type_id`**: hay varios, y una vez terminó en el `Tick`.
- ⚠ **`Class|<BP>|Set<Var>` puede colisionar** con un nodo del motor del mismo nombre (`SetLabelText` agarró uno de **Niagara**). Ante la duda, un setter propio con nombre único.
