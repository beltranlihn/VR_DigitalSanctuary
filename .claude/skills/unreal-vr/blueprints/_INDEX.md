# 🗺️ Mapa de Blueprints del proyecto Soul Charger

Índice de **todos los Blueprints que existen** en el proyecto: qué es cada uno, dónde está, para qué sirve y su estado. El **detalle profundo** (variables por categoría, estructura de grafos, qué palanca ajusta qué) vive en el **tracker por-BP** de esta misma carpeta (`blueprints/<BP>.md`); este archivo es el **índice que los mapea**.

> 🔴 **REGLA (mantener vivo):** cada vez que crees o modifiques un Blueprint, **actualizá su fila acá** (estado, propósito) **y su tracker** (`blueprints/<BP>.md`). Si un BP no tiene tracker y lo vas a tocar, crealo (modelo: `BP_BreathSensor_V2.md`, plantilla: `_TEMPLATE.md`). Así, al retomar un BP viejo, sabés su estado sin releer el grafo.
>
> **Leyenda estado:** 🟢 funcional/validado · 🟡 en progreso · 🧩 stub/scaffold vacío · ⚪ existe pero sin auditar (completar al tocarlo) · 🗑️ deprecado.
> **Tracker:** ✓ = tiene tracker detallado · — = todavía no.

---

## 🧼 ETAPA ACTUAL (desde 2026-08-17): la VERSIÓN LIMPIA, en `MapsV2/L_SoulCharger`
🔴 **El nivel de trabajo es `/Game/SoulCharger/MapsV2/L_SoulCharger`, no `L_Persistent`.** La etapa consiste en **rehacer las mecánicas de forma limpia y ordenada en un nivel nuevo** (pedido de Beltrán: *"empezar a armar algo más en limpio"*). El esqueleto viejo **no se toca**: queda como referencia de lo que ya está probado, y de ahí se copian los criterios, no los archivos.

| Ya rehecho en limpio | Reemplaza a | Estado |
|---|---|---|
| [`BP_Director_Movement`](BP_Director_Movement.md) — recorrido + caminata + pasos + viñeta, todo en un BP | `BP_Journey` + `BP_Walker` + `BP_Vignette` | 🟢 verificado por log, falta visor |
| [`BP_Director_Music`](BP_Director_Music.md) — ambientes con crossfade por tramo | (no existía) | 🟢 verificado por log |
| [`BP_Door_SC`](BP_Door_SC.md) + `WBP_DoorTitle` — puerta con arte real y título en arco | `BP_Door` | 🟡 5 colocadas, falta visor |
| [`BP_Director_Rooms`](BP_Director_Rooms.md) — carga/descarga de sublevels + fundido por material | `BP_StageDirector` | 🟢 ciclo de 6 salas verificado por log |
| 🆕🎬 [`BP_Director_Story`](BP_Director_Story.md) — **el director del GUIÓN**: la maqueta de principio a fin con los 33 VO de Alma como anclas (2026-08-19) | `BP_StageDirector` + `BP_Stage_*` + `BP_Ceremony` | 🟢 recorrido completo Hall→final verificado en PIE por log en `bAutoTest`; falta visor |
| 🆕💡 [`BP_LightShaft_SC`](BP_LightShaft_SC.md) + `M_LightShaft` — **el haz de luz falso**: cono/cilindro unlit aditivo que finge luz sobre humo (Fresnel invertido + degradado + senos + DepthFade), sin luces ni fog (2026-08-20) | (no existía; el toolkit Turrell) | 🟡 colocado tras el PlayerStart; falta juicio en editor/visor |
| 🆕🎛️ [`BP_Sensor_Soul`](BP_Sensor_Soul.md) — el sensor de la mano hábil, **contenedor único de las mecánicas de interacción** (2026-08-24): `SetStage(n)` activa por sala — 1 = **respiración por umbral con valores BASE, SIN calibración** (pivote de Beltrán: los valores finos llegan con los datos del levantamiento; la maquinaria de calibración quedó dormida), 2 = latido al BPM del BioHub, 4 = beam desde el Aim (`M_Beam_SC`, sólo visible en su etapa), 5 = 🆕 **dibujo 3D libre** (2026-08-26, Surrounding V2: punta = el propio sensor + One-Euro, paleta `BP_BrushPalette` en la mano contraria, 1 m de práctica cierra las instrucciones sin botón, 10 m cierran la etapa, el dibujo se disuelve vía `MPC_Draw` y reaparece como FIRMA en `TP_signature_spot` en el final; `BP_DrawCanvas` se spawnea INTACTO como motor de geometría — plan `docs/stages/surrounding-v2.md`). En Entering el umbral se arma **desde las instrucciones** en modo práctica (`bPractice`) y sigue toda la etapa; **las etapas cierran SOLO por su duración** (los contadores no cierran nada). Háptico: pulso al entrar al umbral + zumbido continuo dentro (0.25, el valor de V2). La toma se auto-reintenta si el pawn no existe aún. | `BP_Sensor` + `BP_BreathSensor_V2` + `BP_HeartSensor` + `BP_HandPointer` (roles) | 🟡 modos, práctica y ciclo 1-5 verificados en PIE por log y medición directa, cero `Accessed None`; las mecánicas reales necesitan visor |
| 🆕🎬 [`BP_StageIntro_SC`](BP_StageIntro_SC.md) + `WBP_StageIntro_SC` — **la explicación de las etapas del Hall** (2026-08-25): abre con la lámina **"Soul Charger"** (píldora gris + la ameba naciendo animada), que **desaparece y le cede el lugar a Entering**; después el VO nombra las 5 etapas y aparece una **píldora por etapa a la izquierda** (nace con ancho 0 desde su centro y se abre, como el marco del EEG del HUD) mientras la **proto ameba REAL a la derecha carga su anillo**. El grupo de píldoras se mantiene **siempre centrado**: al entrar una nueva, las anteriores se deslizan hacia arriba. 🔴 **Dos fases separadas para que nunca se superpongan**: `Occupy` abre el hueco (velocidad `SlideTime`) y sólo cuando terminó arranca `Reveal`, el ancho (`GrowTime`). 🎨 Color y tipografía se editan **en el Designer** (el código sólo escribe posición/ancho/opacidad); diagramación (`PillW/PillH/PillGap`) y tiempos (`StepTime`/`GrowTime`/`SlideTime`/`RingTime`) son variables. `IntroPlay()` es la API para engancharlo al VO. | `BP_StageIntro` (esqueleto viejo) | 🟢 secuencia y centrado verificados en PIE por medición; falta visor y enganche al VO |
| 🆕🌬️ [`BP_BreathOrb_SC`](BP_BreathOrb_SC.md) — **la esfera que se controla con la respiración en Entering** (2026-08-24): consumidor tonto colocado en `L_Entering_SC` que lee `BP_Sensor_Soul`; **aparece cuando terminan las instrucciones** (durante ellas el usuario practica con el **círculo del widget**, páginas **2-3** de `WBP_Instructions`, textos en inglés) y se va al cerrar la etapa por tiempo. Crece a 0.45 al inhalar sostenido dentro del umbral, base 0.25. | `Box_Breath` | 🟡 ciclo `ORB: true → false` y `RevealT` 0→1 verificados en PIE por log y medición; la respiración real necesita visor |
| 🆕⏱️ [`BP_BreathRing_SC`](BP_BreathRing_SC.md) + `WBP_BreathRing_SC` + `M_BreathDot_SC` — **el temporizador de respiración de Entering** (2026-08-25): dos anillos concéntricos — uno fijo que es el ciclo completo y uno animado que **crece al inhalar, queda quieto al aguantar y se achica al exhalar** —, medios discos que marcan las divisiones (panza **hacia el centro**), un marcador que avanza en sentido horario y los nombres de cada tramo **escritos sobre la curva**. 🔴 **Es un reloj fijo, NO reactivo al usuario**, y sobre todo es **configurable**: `Divisions` 2/3/4, `PhaseTimes`, `CycleTime` y `Cycles` en el panel, y **divisores, textos y tiempos se reacomodan solos en el viewport, sin Play**. Los arcos son **proporcionales a la duración** (decisión de Beltrán), así el marcador va a velocidad constante y cae exacto en cada divisor. Geometría medida del PDF `BreathRing.pdf`. Reusa **`M_SoulRing`** (los anillos); las palabras son **texturas horneadas curvas** (`M_BreathWord_SC` + `MI_Word_*`), idea de Beltrán: 4 planos concéntricos que sólo se **rotan** — se ven en el viewport sin Play y cuestan 4 draw calls. 🆕 **Es el reloj de la etapa Entering**: entra dibujándose cuando terminan las instrucciones (`BRingShow`), arranca los ciclos cuando termina el VO (`BRingGo`), y al completarlos sale borándose y llama al `StepTimeDone` del director — lo que apaga el umbral, saca el orbe y sigue el cierre — y **se destruye**. El cortafuegos por tiempo sigue vivo: cierra el que llegue primero. El director lo maneja con **`BreathRingCue(Mode)`**, enganchada por cirugía en `StartStepTime` y en `OnVOFinished_Event`. | [`BP_BreathPacer`](BP_BreathPacer.md) | 🟡 layout, ciclo (3.001 s), animación del anillo **verificados por medición** en editor y PIE, cero `Accessed None`; dos correcciones visuales de Beltrán ya aplicadas (sentido horario, medios discos al centro); falta visor y el arte de las palabras |
| 🆕🛗 [`BP_Elevator_SC`](BP_Elevator_SC.md) — **el ascensor de Recognizing** (2026-08-25): el pawn quieto y **los meshes tagueados `rise` suben** — cada 1/2 latido del usuario en la zona de pecho ([[BP_Sensor_Soul]] modo 2, `OnBeatPulse`) da un **kick de velocidad que decae**; fuera del umbral con el viaje iniciado sigue a `CoastSpeed` lento. La distancia la marcan **dos muros tagueados `rise_top`/`rise_bottom`** (cambiar el recorrido = arrastrar el de abajo); al completarla llama `StepTimeDone` (mismo patrón que el anillo de Entering; el cortafuegos `StepTimes[2]`=240 s convive). 🔴 Fuerza `Movable` en el boot (los StaticMeshActor nacen Static y el offset falla en silencio) y **no mueve hijos cuyo padre ya está en la lista** (el muro duplicado estaba attacheado → subía 2×). El feedback vive en el sensor: zumbido continuo en zona + pulso + **audio `HeartBeat`** por latido + el **círculo en la página 4 del panel** (visible solo en umbral, salta con `BeatEnv`). | `BP_Descent` + `BP_HeartSensor` (roles) | 🟢 cadena completa verificada en PIE por log y medición (beats→kicks→subida 1:1→cierre); falta visor |
| 🆕🕸️ [`BP_NeuralWeb_SC`](NS_NeuralWeb_SC.md) + `NS_NeuralWeb_SC` + `M_NeuralDot_SC`/`M_NeuralLink_SC` + `MPC_NeuralWeb` — **la red neuronal de Loving** (2026-08-26): puntos unidos por líneas que se deforman con curl noise, con **conexiones que nacen y mueren** tomando pares nuevos (la red se reconfigura sola) y **nacimiento/muerte desde el centro** (`WebIn()`/`WebOut()`, se autodestruye y avisa por `OnWebGone`). 🔴 **Arquitectura**: el BP calcula la topología UNA vez (posiciones de reposo + pares por vecindad, seed determinística) y Niagara solo COLOCA — nodos como sprites, líneas como **mesh orientado con `OrientMeshToVector`** —; **todo el movimiento vive en el vertex shader**, así que **se anima en el viewport sin Play**. Forma **esfera o toroide** (`Shape`), tamaño de puntos con variabilidad (`DotMin`/`DotMax`), y la calma del EEG modula la amplitud del noise. 🔴 **`NoiseFreq` es la palanca crítica del look**: si la longitud de onda es menor que la red, las líneas se estiran y se ve como una maraña. Sin Scratch Pad ni Attribute Reader (el MCP no puede editarlos). | los 3 `NS_LovingField` (rol de arte del Acto 6) | 🟡 verificado en PIE y editor, cero `Accessed None`; ⬜ **falta enganchar al director** (aparecer tras el VO de Loving, salir al terminar el step time y recién ahí cerrar la etapa) + visor |
| 🆕🎵 [`BP_Sequencer_SC`](BP_Sequencer_SC.md) + `BP_SoundOrb_SC` + `BP_SeqSlot_SC` + `BP_SaveMelody_SC` — **la etapa Attracting limpia** (2026-08-26, `Core/Attracting/`): secuenciador de **8 pasos alineado al pad por construcción** (`StepDur = Duration(PadM1)/8` = 0,667 s; el pad de 5,333 s arranca junto con el paso 0), esferas con clip (`ModuleSounds` = los 20 M1S*) que se apuntan con el **beam de [[BP_Sensor_Soul]]** (hover=preview, gatillo=far-grab, soltar=colocar/swap/casa — los patrones probados de `BP_SoundBubble` con sus números), slots que laten con el playhead, y botón **SAVE MELODY** (apuntar+hold 3 s, disponible desde el 1er slot). 🔴 **La instrucción es 1 página SIN botón**: aparece UNA esfera junto al panel y **arrastrarla a un slot termina las instrucciones** (`NotifyPlaced` → `panel.Finish()` → nacen las 12 esferas + pad). El cierre es el botón: esferas sueltas fuera, la mesa viaja al frente (`FinalOffset`), **2 pasadas del loop** y `StepTimeDone` → la carga. Cortafuegos `StepTimes[4]`=300 + watchdog (`Sensor.Mode≠4` → cierra solo). | `BP_AttractDirector` + `BP_SoundBubble` + `BP_SeqSlot` + `BP_SaveButton` + `BP_AimBeam` (roles) | 🟢 **MECÁNICA COMPLETA VALIDADA EN VISOR por Beltrán (2026-08-26, iteración en vivo)**: 2 beams Niagara (`LineTrace`), grab/háptico por mano, snap con zona visible (`SnapZone`+`ZoneRadius`), colores de estado, intro colocar+guardar con `IntroDelay`, SAVE = fin de interacción, destino final por `TP_seq_final_attracting`. ⬜ apagar prints de diagnóstico; ver el cierre de jornada del tracker |
| 🆕📊 [`BP_SoulHUD_SC`](BP_SoulHUD_SC.md) + `WBP_SoulHUD_SC` + `M_HudWidget_SC` — **el HUD** (2026-08-24): barra de carga, gráfico EEG y punto del latido en **un solo widget** sobre un lienzo de 1000×400 (= 40×16 cm, holgado a propósito para que nada se recorte). 🔴 El trazo del EEG **sigue al marco `GraphArea`** vía `SyncBox` (geometría cacheada) → se arrastra en el Designer y la onda se acomoda sola. Material propio con **Disable Depth Test** (params obligatorios `SlateUI`/`TintColorAndOpacity`/`OpacityFromTexture`) para que quede por encima de todo, como la ameba. Señal desde el **BioHub colocado en el world** (`bFakeSignal`), carga **derivada** de `Room` del director (+20 % por etapa). Sin fondos. ⚠ `GeometryMode=Cylinder` probado y descartado: no dibuja con la escala 0.04 del componente (sí a escala 1, donde el radio queda inservible). | `BP_SoulHUD` | 🟡 verificado en PIE (onda dibujando, attach a la cámara); falta visor y diagramación fina |

- **6 sublevels** en `MapsV2/RoomsV2/` (`L_{Hall,Entering,Recognizing,Loving,Attracting,Surrounding}_SC`), registrados en el persistente y **descargados al arrancar** (`bInitiallyLoaded`/`bInitiallyVisible` en false): los carga y descarga `BP_Director_Rooms`.
- **Cada sala tiene su arquitectura**: `Cylinder` (piso) + `Cylinder_001` (muro) de `Asset/RoomBase/`, en su posición mundial (0 · 1500 · 3000 · 4500 · 6000 · 7500, yaw 90) y con **sus propias instancias de material** `MI_<Sala>_Muro` / `MI_<Sala>_Piso` en `Asset/<Sala>/`, para color y textura por sala.
- 🔴 **El fundido de la obra pasa por `MPC_Room.RoomLight`** — un escalar global 0-1 que multiplica `BaseColor` y `Emissive` de `M_DoorSolid`, `M_DoorGlass` y `M_RoomInterior`. Una sala que entra con `RoomLight = 0` **nace negra**, sin destello en el primer frame. El exterior (`M_RoomExterior`, unlit sin parámetros) queda negro puro siempre.
- 🔴 **Los valores de instancia que ajusta Beltrán son datos de autor** (posiciones de puertas, tiempos de tramo, mezcla). Reponer un actor los borra: leerlos y subirlos al CDO **antes**. Ver `gotchas.md` §114-115.
- 🎬 **El director de etapas de la versión limpia YA EXISTE: [`BP_Director_Story`](BP_Director_Story.md)** (2026-08-19). Secuenciador por pasos con `WaitFor`; VO indexados por el número de Beltrán (`BP_Alma_SC.VOClips[i]` = VO i, 0 y 6 vacíos); tags derivados de `RoomNames` (`alma_<sala>_appear/_move`, `instr_<sala>`, `soul_pick_<n>`). **Quién avisa qué**: `Alma.OnVOFinished` · `Move.OnLegStarted` (→ detección de cruce de puerta por X) · `Picker.OnChosen` · `Sensor.OnTaken` · `Panel.OnFinished` · `Winner.OnArrived/OnRingDone`. Pendiente: **visor**.
- 🎯 **2026-08-21 — el alma del usuario, anclada a la cabeza de verdad + modo HUD.** Tres cosas en [`BP_ProtoSoul_SC`](BP_ProtoSoul_SC.md): **(1)** el anclaje a la cara pasa de *copiar la transform en el Tick* (un frame de retraso en VR = "lazy follow") a **attach real a la `CameraComponent` del pawn** — medido con `find_actors(root=pawn)`: el TargetPoint `soul_face` **no** cuelga de la cabeza, así que attachearse a él no alcanzaba. **(2)** **Modo HUD** (`bHudOnTop`): mientras está anclada, cuerpo y anillos se dibujan **por encima de todo** (las manos no la tapan, como el overlay de OVR Metrics) vía `Disable Depth Test` — que **sí** corre en el renderer móvil. Cuerpo por swap a `M_ProtoSoul_HUD`; anillos con la opción siempre activa en `M_SoulRibbon_SC`. **(3)** `PushBodyMat()` repone los 22 parámetros del Construction Script **+ `FloatScale`** al cambiar de material, porque el swap tira el MID. 🔧 **Volver atrás:** `bHudOnTop=false` (cuerpo) y destildar Disable Depth Test en `M_SoulRibbon_SC` (anillos).
- 🗂️ **2026-08-21 — el outliner de `L_SoulCharger` quedó ordenado en carpetas** (antes: 47 actores planos, con 20 `TargetPoint_N` indistinguibles):
  `01 - Directores` · `02 - Alma (la guia)` + `/Puntos` · `03 - Alma del usuario` + `/Candidatas` + `/Puntos` · `04 - Partida (pantalla de inicio)` · `05 - Entorno`.
  🔴 **Convención: los TargetPoint se LLAMAN como su tag, con prefijo `TP_`** (`TP_alma_loving_appear`, `TP_soul_pick_3_loving`). Renombrarlos es **seguro** porque todo el código los busca por **tag**, nunca por nombre — verificado leyendo los tags después del renombrado. **Al agregar un punto nuevo, ponerle el nombre de su tag.**
  ⚠ Los actores de motor (`WorldSettings`, `Brush`, `BuoyancyManager`, `DefaultPhysicsVolume`, `AbstractNavData`…) **no aceptan carpeta**: `set_actor_folder` devuelve éxito y no hace nada. Quedan en la raíz; no molestan porque el outliner los filtra.
- 🧪 **2026-08-21 — herramienta de debug: empezar la obra en cualquier sala.** `DebugStartRoom` (int instance-editable en `Director_Story`, **−1 = obra normal**, 0-5 = salta a esa sala) + `BP_Director_Rooms.DebugJumpTo(Index)`. Salta **por el flujo real** y **siembra lo que el usuario ya habría hecho**: sensor tomado, proto ameba elegida (por el `OnChosen` de verdad), anclada a la cara, y `Room−1` anillos de las etapas vividas. Arranca **cuando la sala entra desde el negro** (poll de la fase 3 del director de salas, no un delay: la carga midió entre 0,58 s y 1,67 s), así los VO de Alma suenan mientras la luz sube. Detalle en [`BP_Director_Story.md`](BP_Director_Story.md) y [`BP_Director_Rooms.md`](BP_Director_Rooms.md). 🔴 Dejar en **−1** al terminar.
- 🆕 **2026-08-21**: reporte del APK → `r.PSOPrecaching=1` y `r.MaxAnisotropy=8` en `Config/Android/AndroidEngine.ini` (el salto al cargar el Hall era compilación de PSO, no streaming; el piso borroso era falta de anisotrópico), la **viñeta se oculta** cuando no actúa, y **el fondo de las instrucciones dejó de ser el color del widget**: ahora es geometría (`Glass`, ProceduralMesh curvado igual que el widget) con **`M_InstrGlass`** + 5 instancias de color en `Core/UI/`. Gotcha nuevo **§183**: el Construction Script lee propiedades de COMPONENTES del CDO, no de la instancia.
- 🆕 **2026-08-20**: Beltrán corrió la obra entera en editor ("funciona perfecto"). Se sumaron: `VODelayIn`/`VODelayOut` en Alma (aire configurable alrededor de cada VO), `MaxLag` en ProtoSoul (el lazy follow ya no se rezaga en caminatas), el sonido del botón de instrucciones al hacerse VISIBLE (no al entrar a la sala), y el **final rediseñado**: modo compartir (hover + gatillo sostenido = a la mano; soltar = vuelve; acercarla bajo el visor = compartida → `soul_pick_6`) y tras VO 33 → step time → **camera fade con audio → recarga del nivel**. ⚠ El camera fade falta confirmarlo en visor (plan B: esfera de fade).
- Cambios de esa jornada en BPs ya existentes: `BP_Alma_SC` (VO + `bStartHidden`), `BP_ProtoSoul_SC` (`OnArrived`/`OnRingDone`/`EnableHover`), `BP_SoulPicker_SC` (`ChosenTag`=`soul_pick_0`, `OnChosen`, `Rearm`, `ForceChoose`), `BP_InstructionsPanel_SC`+`BP_InstrButton_SC` (`bStartHidden`/`Show()`), `BP_Director_Rooms` (`CloseRoom()`). Los 7 TargetPoints de pick quedaron tagueados `soul_pick_0..6` y a escala **0,3** (antes todos `soul_pick_0` y escala 1 = ameba de 1 m).

---

## Core — infraestructura compartida (`Content/SoulCharger/Core/`)
> ⚠️ Estos son **compartidos entre stages** → coordinar antes de tocar (ver `docs/WORKFLOW-EQUIPO.md`). Varios están **sin auditar en detalle** — auditar y documentar al primer trabajo sobre ellos.

| Blueprint | Ruta | Qué hace | Estado | Tracker |
|---|---|---|---|---|
| **BP_VRPawn_SC** | `Core/Pawn/` | El pawn VR de la obra (derivado del VR template; obra sentada). | ⚪ | — |
| **BP_SoulChargerGameMode** | `Core/Flow/` | GameMode; `DefaultPawnClass = BP_VRPawn_SC`. | ⚪ | — |
| **BP_FlowDirector** | `Core/Flow/` | 🔴 **AUDITADO 2026-08-11: es un SMOKE TEST, no el director.** Delays hardcodeados de 5/8/3 s que cargan `L_Test_Stage` y hacen un fade. Solo 2 variables. **Valor real: probó que `LoadLevelInstance(byName)` y `BP_FadeSphere.StartFade` funcionan.** Tiene el bug de llamar `LoadLevelInstance` **sin** `OptionalLevelNameOverride` → filtra un paquete nuevo por llamada. Lo reemplaza `BP_StageDirector` (§9.4). | 🗑️ a deprecar | — |

## 🦴 Esqueleto de la obra — paso 1 del orden de construcción (`Core/`, rama `core/esqueleto`)
> Nivel persistente + caminata entre salas. Es el **mayor riesgo técnico de la obra** y todo lo demás cuelga de ahí (`docs/OBRA-SOUL-CHARGER.md` §10). Empezado 2026-08-11.

| Blueprint | Ruta | Qué hace | Estado | Tracker |
|---|---|---|---|---|
| **BP_Room** | `Core/Rooms/` | La sala placeholder: piso disco de 10 m con patrón + cilindro de muro de 4,5 m sin techo, en el origen. `SetLight(Alpha)` sube y baja su luz; `Configure(Nombre, Acento)` la pinta desde el director. 🆕 `Dissolve(WaitTime)`: el caso terminal — apaga y **esconde** piso y muro para devolver el exterior. Se instancia como **streaming sublevel**: **6 mapas propios** (`Maps/Rooms/L_Room_{Hall,Entering,Recognizing,Loving,Attracting,Surrounding}`). 🔴 2026-08-13: **cada mapa guarda su sala EN SU POSICIÓN MUNDIAL real** (0·1200·…·6000, puerta +460) — el mapa es la autoridad; el director no mueve nada; parada del Journey y preview deben coincidir. Beltrán diseña cada sala en su mapa, en sus coordenadas reales. | 🟡 falta visor | [✓](BP_Room.md) |
| **BP_Walker** | `Core/Movement/` | La caminata: spline + rampa smoothstep + bob vertical y giro **acoplados a la cadencia del paso** + viñeta. Rango por tramo (`StartWalk(From, To, RampIn, RampOut)`) porque la caminata son **dos tramos de 5 m** con el negro en el medio. Todo parametrizable, incluido a cero. Vive en el persistente. | 🟡 falta visor | [✓](BP_Walker.md) |
| **BP_Vignette** | `Core/UI/` | Viñeta de comodidad: esfera pegada a la cámara (patrón de `BP_FadeSphere`), máscara **geométrica** (correcta en estéreo, no screen-space). Sin colisión para no tapar los line traces. | 🟡 falta visor + medición | [✓](BP_Vignette.md) |
| **BP_Door** | `Core/Doors/` | §9.8: dos paneles de 3×4 m con 10 cm de espesor, cartel `TextRender` con `StageName`, plano negro detrás, marco que **se traza** con barras que crecen. `Reveal`/`Open`/`Close`/`Configure` + `OnPawnPassed` por producto punto (no por overlap: el pawn se mueve con SetActorLocation). Una instancia en `L_Room_Placeholder` en X=460. | 🟡 falta visor | [✓](BP_Door.md) |
| **BP_StageDirector** | `Core/Flow/` | §9.4/§9.2: el ciclo **completo** precarga invisible → baja la luz + traza el marco → caminar hacia la **puerta CERRADA** → abre al llegar (`DoorOpenDelay`) con el negro cayendo → swap + reposiciona el pawn → sube la luz. Encadenado con `SetTimerByFunctionName`. 🆕 **2026-08-12 (revisión): 6 SALAS** (Hall + 5 etapas; el final NO es sala) vía **`RoomMaps`** (un `.umap` por sala, indexado por `PreloadNext(Suffix)`); `FinishObra` → **`BP_Room.Dissolve`**: la arquitectura se deshace en el lugar y queda el exterior. Serial monótono para las instancias y descarga bajo negro. 🧪 **2026-08-14: `DebugStartStage`** (int instance-editable, −1 = obra completa; 0..5 = al dar play salta directo a esa etapa por el flujo real en ~2,6 s, sembrando sensores + mano hábil). | 🟡 3 recorridos completos verificados por log, falta visor | [✓](BP_StageDirector.md) |
| 🆕 **BP_Alma** | `Core/Amoeba/` | La GUÍA de la obra (≠ Proto Soul, decisión 2026-08-12). Placeholder: esfera unlit cálida de 35 cm (M_ProtoSoul), sin colisión, spawneada en `TP_Alma` por el Hall. La entidad real llega con arte + VO. | 🧩 placeholder funcional | — (ver BP_Stage_Hall.md) |
| 🆕 **BP_Anchor** | `Core/Debug/` | **El punto de spawn que se ve en el editor**: hereda de `TargetPoint` (por eso todas las búsquedas por tag siguen andando sin tocar nada), con esfera fantasma + su tag rotulado por Construction Script, ambos **Hidden in Game**. Los 14 TargetPoints de `L_Persistent` son ahora anchors. | 🟢 verificado en PIE | [✓](BP_Anchor.md) |
| 🆕 **BP_Journey** | `Core/Movement/` | **El recorrido completo de la obra en UN spline** con 8 paradas que se arrastran en el viewport + `LegTimes` por tramo (con fallback, agregar puntos no rompe). API por *input key*, nunca por distancia. 🆕 2026-08-13 (narrativa): paradas `[−2300, −560, 0, 1200..6000]` — aparición lejos en el vacío, llegada frente a la puerta del Center, Hall calzando con su preview. El director lo usa para TODO (corredor incluido). | 🟢 verificado en PIE por log; falta visor | [✓](BP_Journey.md) |
| 🆕🧼 **BP_Director_Movement** | `Core/Movement/` | **La versión LIMPIA del recorrido: spline + caminata + pasos en UN solo BP** (sucede a `BP_Journey` + `BP_Walker`, que siguen vivos en el persistente viejo). Vive en `L_SoulCharger` (`MapsV2`). 9 paradas de −5000 a 7500 que se arrastran en el viewport; **duración en segundos por tramo** (`TimeLeg0to1`/`1to2`/`2to3` + `TimeLegRest` para el resto) y **`LegWalkIntensity` = intensidad del efecto por tramo** (la primera en 0: del punto 0 al 1 no hay caminata ni pasos). Salidas y llegadas por **smootherstep** (velocidad *y* aceleración cero en los extremos: se detiene del todo sin tirón). Pasos = loop `Pasos` que **se spawnea por tramo y se destruye al terminar el fade** (`StepsFade` 0,2 gobierna fade in, fade out y kill) — sin AudioComponent permanente. 🆕 **Viñeta propia adentro del BP** (componente + `M_VignetteWalk`, duplicado del validado; `BP_Vignette` queda intacto para `BP_Walker`): se attachea a la cámara y sigue la **velocidad real**, no la intensidad del efecto — por eso también cubre el tramo sin caminata. `GoToNext()` es el único punto de entrada del avance. 🧪 **La tecla `1` avanza un tramo** (`WasInputKeyJustPressed` en el Tick: sin IMC, sin Enhanced Input) y `bAutoAdvance` recorre todo solo. | 🟢 recorrido y tiempos verificados en PIE por log; falta visor | [✓](BP_Director_Movement.md) |
| 🆕🚪 **BP_Door_SC** | `Core/Doors/` | **La puerta entre salas, con el arte real** (`Asset/Door/`): dos hojas que corren de lado a lado (`OpenWidth` 114 = vano libre), vidrio translúcido que va **de negro a `AccentColor`**, `DoorOpen` sonando **en la puerta**, y un **widget** con el título de la sala (`StageName`, por instancia). Se abre **por distancia al pawn** (`OpenDistance`), con `OpenTime` y **`EasePower`** (1 lineal · 2 suave · 3+ marcado) para calzarla con la pasada. 🔴 **Cada puerta vive DENTRO del sublevel de su sala** — el mapa es la autoridad de posición y así viaja con el streaming. Ya colocadas las 5 (Entering→Surrounding); la del Hall será otro BP. | 🟡 compila y arranca sin errores en las 5; falta visor | [✓](BP_Door_SC.md) |
| 🆕🚪🧼 **BP_Director_Rooms** | `Core/Flow/` | **La carga y descarga de salas de la versión limpia** (2026-08-17). Corre la transición entera: fundido a negro de TODO el mundo por `MPC_Room.RoomLight` → descarga del sublevel actual → carga del siguiente → **encendido de las luces del interior** → `GotoNext()` del recorrido. 🔴 **El fundido no es una esfera negra: es un escalar de Material Parameter Collection** que multiplica `BaseColor` y `Emissive` de los 3 maestros — por eso la sala que entra **nace negra** y no hay destello en el primer frame. La espera del negro es **por el completado real del streaming** (mínimo `BlackHold`), nunca por delay fijo. El **Hall entra por llegar a la parada `HallStop` del spline**; el resto por `EndStage()` (hoy la tecla `2`). `bAutoDemo` recorre las 6 salas solo. | 🟢 ciclo de 6 salas verificado en PIE por log, una sola cargada a la vez, cero `Accessed None`; falta visor | [✓](BP_Director_Rooms.md) |
| 🆕✨ **BP_Alma_SC** + **M_Alma** + **SM_AlmaSphere** | `Core/Alma/` | **La GUÍA de la obra, versión limpia** (2026-08-18): esfera-ameba translúcida que flota, aparece y viaja entre TargetPoints tagueados. 🔴 **Toda la vida está en el MATERIAL**: deformación, gradiente, doble Fresnel y el flotar son **sumas de senos con frecuencias no enteras** en WPO — se descartó el nodo `Noise` con los números de Epic (16-80 instr. **por octava**). El BP no tiene Tick para el flotar y **se ve animada en el editor**. Malla propia: **icoesfera de 5.120 tris** (la del motor tiene los polos pinchados). API: `AppearAt(tag)` · `MoveTo(tag)` · `Disappear()`, **sin spawn/destroy**. Rig de test en teclas 3/4/5. | 🟢 verificado en PIE por Beltrán | [✓](BP_Alma_SC.md) |
| 🆕🔔 **BP_Bell** | `Core/Doors/` | **El timbre que abre la puerta** (2026-08-18): cilindro de 35 cm que se apoya **con la mano, sin gatillo** (proximidad, la gramática de [[BP_Sensor]]) y que al **sostener 3 s** abre todas las `BP_Door_SC` con su actor tag (`BellTag`, default `bell`). El cuerpo se hunde y **el anillo se queda quieto**; soltar antes vuelve el anillo a 0. Nace en escala 0 y **crece al acercarse el pawn**; al disparar abre la puerta, avanza el recorrido, se encoge y **se autodestruye**. 🔴 **Se COLOCA a mano en el sublevel** (se descartó el spawn por TargetPoint). El anillo es un **`RadialSlider`** en `WBP_BellRing` sobre un `WidgetComponent`. | 🟡 compila limpio; el hover se arregló (nodo podado) pero **falta visor** | [✓](BP_Bell.md) |
| 🆕🎵 **BP_Director_Music** | `Core/Audio/` | **El ambiente de la obra**: array `AmbientClips` (los 9 de `Core/Audio/AmbientClips/`, la entrada *i* = la parada *i*, editable sin tocar código) + **crossfade real** entre uno y otro, disparado por el arranque de cada tramo de [[BP_Director_Movement]] (flanco de `bWalking`; el dispatcher `OnLegStarted` también existe). Cada clip se spawnea, entra con fade y **el anterior se libera cuando su fade termina**. `CrossfadeTime` y `MasterVolume` (en vivo) son las palancas. 🔴 **Estéreo head-locked a propósito**: es el score, no un objeto de la sala — y en 5.8 no hay spatializer de Meta ni HRTF propio. | 🟡 compila con los 9 clips; falta colocarlo y probarlo | [✓](BP_Director_Music.md) |
| 🆕 **BP_Stage_Entering** | `Core/Stages/` | La primera etapa REAL (paso 4): integra la cadena probada de Breath (`BP_Instructions` 5 páginas → `BP_BreathSensor_V2` → `Box_Breath`) al ciclo de salas; cierra por respiraciones completas (`bStageComplete` → `StageDone`) o por el cortafuegos EXTENDIDO (`ExtendTimeout` 240 s). El director la spawnea en el índice 1. | 🟡 ciclo verificado por log; la respiración necesita visor | [✓](BP_Stage_Entering.md) |
| 🆕📖 **BP_InstructionsPanel_SC** + **WBP_Instructions** | `Core/UI/` | **Instrucciones por etapa** (2026-08-18): un solo widget con TODAS las páginas y un contenedor que elige el **rango** (`StartIndex`/`EndIndex`) y el color de fondo por nivel. 🔴 Las páginas son un **`WidgetSwitcher`**, no un array: cada hijo es un Canvas que se diseña y se previsualiza en el Designer con `activeWidgetIndex`, y sólo el hijo activo se renderiza (ergonómico **y** barato). API `Apply(Index)` / `Step(Delta)`. Se ve en el editor por Construction Script. **Faltan los botones físicos → reusar `BP_Bell`, no rehacerlo.** | 🟡 construido, falta mecánica y visor | [✓](BP_InstructionsPanel_SC.md) |
| 🆕💡 **BP_TurrellPanel_SC** + **M_TurrellGradient** | `Core/Light/` | **Luz de Turrell sin luces** (2026-08-18): plano con degradado radial de 3 colores, animado y emisivo, para transformar la arquitectura con un shader en vez de luces en tiempo real. El material es **genérico** (sirve en cualquier malla con UV0). 🔴 Resuelve las **dos** quejas históricas de los degradados animados en VR, que son problemas distintos: el **movimiento cortado** es fp16 → `floatPrecisionMode = MFPM_Full_MaterialExpressionOnly` + `Time.period = 300`; el **banding** es la cuantización a 8 bits → dither R2 en espacio de pantalla (`DitherTemporalAA` NO sirve, en Quest no hay TAA). Emisivo **sin quemarse a blanco** por normalización al canal máximo. Sin Tick, sin funciones, sin variable `MID`. Una instancia en el centro del hall. | 🟡 verificado en editor, falta visor | [✓](BP_TurrellPanel_SC.md) |
| 🆕✨ **BP_ProtoSoul_SC** + **BP_FaceAnchor_SC** + **M_ProtoSoul** | `Core/ProtoSoul/` | **El alma personal del usuario, version limpia** (2026-08-18): duplicado de `BP_Alma_SC` con material propio. Malla intercambiable, giro lento y erratico sobre el `Body`, y **hover con la mano** que enciende el contorno moviendo `FresnelPower` (donde vive el color), con histeresis para que no parpadee. 🔴 **Su sistema de puntos guarda la REFERENCIA al TargetPoint (`TargetRef`), no una coordenada**, y relee su transform cada frame con `VInterpTo`/`RInterpTo`/`FInterpTo`: asi sigue un ancla en movimiento sin ningun attach **y el tamano sale de la escala del punto**. `BP_FaceAnchor_SC` es el doble del HMD que permite autorar la pose frente al ojo en el editor. 🔴 **5 instancias COLOCADAS en el persistente** con tag `soul_pick` — malla, color y tamaño son datos de autor **en la instancia**. Nacen dormidas (`bStartAsleep`). API: `Sleep()` · `Reveal()` · `Select(FaceTag)` · `MoveTo(tag)` · **`MoveToActor(actor)`** (2026-08-27: anclarse a un actor cualquiera — posición **y tamaño**; lo usa la constelación) · `Disappear()` · `Configure(malla, color)` · `SeedRings(n)`. 🆕 **El gesto de compartir** (2026-08-27, F3): `CarryBody` lleva la ameba a la mano y `ShareZone` exige **sostenerla 3 s** (`ShareHold`) en la zona del corazón antes de disparar `Shared()`. 🐛 Arreglado el 2026-08-19: `Disappear()` **nunca fue terminal** — `ApplyHoverScale` y `StepAppear` se peleaban por la escala del `Body` y la ameba reaparecía un frame después de irse. | 🟡 probado en Play; falta visor | [✓](BP_ProtoSoul_SC.md) |
| 🆕🔮 **BP_SoulPicker_SC** | `Core/ProtoSoul/` | **La pantalla de "Start experience": elegir el alma entre 5** (2026-08-19). 🔴 **Las 5 candidatas NO se spawnean: estan COLOCADAS a mano en el persistente** con el actor tag `soul_pick` — asi la malla y el color de cada una se autoran en el viewport y se ven sin darle Play. En `BeginPlay` las **encuentra por tag y las DUERME** (`Sleep()`); **`Awake()` es el punto de entrada publico** que las despierta cuando la obra llega al Hall (hoy lo suple el andamio `bAwakeOnStart` + `AwakeDelay` 3 s). La eleccion es **hover + gatillo**: la hovereada suena, apaga su hover y viaja a `soul_face`; las otras cuatro se desvanecen y **se destruyen** (`SetLifeSpan`). 🔴 El gatillo son los eventos **`IA_Shoot_Right`/`IA_Shoot_Left` en `Started`** + `AddMappingContext(IMC_MenuTrigger, Priority=1000, bIgnoreAllPressedKeysUntilRelease=False, bForceImmediately=True)` **desde el Tick** — la receta de `BP_MenuButton`, no los defaults. Gatillo sin hover = no-op. 🆕 **Modo compartir por GESTO** (2026-08-27, F3): `Rearm()` enciende `bShareMode` y el Tick escanea **las dos manos por ÁNGULO** (`AimConeDeg` 10°, `AimDwell` 1 s, sin gatillo ni beam); al enganchar apaga el retrato y la ameba viaja a la mano. | 🟡 primer visor OK (spawn, hover, gatillo y viaje a la cara); busqueda + sueno + despertar verificados en PIE, cero `Accessed None`; falta 2º visor y engancharlo al Hall | [✓](BP_SoulPicker_SC.md) |
| 🆕〰️ **M_SoulRibbon_SC** (+ **BP_SoulRing_SC**, banco de pruebas) | `Core/ProtoSoul/` | **El anillo de carga como TRAZO DIBUJADO** (2026-08-19). 🔴 **La obra lo corre desde `ProceduralMeshComponent` NATIVOS dentro de `BP_ProtoSoul_SC`**; `BP_SoulRing_SC` quedó como banco de pruebas suelto tras migrar (el `ChildActorComponent` causó 5 bugs de la misma indirección). Cinta plana con **torsion**, generada por `ProceduralMeshComponent` en el **Construction Script** (se ve y se regenera en el viewport sin Play). El recorrido es un **rollo de 2,5 vueltas superpuestas** — de ahi el look de trazos cruzados. 🔴 **En `Norms` no va la normal sino la DIRECCION DEL ANCHO**: el material es unlit, asi que la normal queda libre como almacenamiento y es lo que permite que el grosor viva en el shader. El material (unlit · **additive** · twosided · full precision) hace crecimiento por `Reveal` 0→1,05 con **punta que ilumina**, respiracion del grosor, pincelada que **nace y muere en punta**, serpenteo y el **flotar compartido con la ameba** (3 senos, razones 1 : 0,73 : 1,37). Additive por estetica: **donde los trazos se cruzan brilla**, y ademas es order-independent (no aplica la trampa de los parches triangulares de `M_Alma`). 442 verts / 440 tris por anillo. 🗑️ Reemplaza a los `Ring0..Ring4` del esqueleto viejo, **descartados por Beltran**. | 🟡 dibuja en el viewport, malla y material verificados; falta arte y visor | [✓](BP_SoulRing_SC.md) |
| 🆕 **BP_Stage_Hall** | `Core/Stages/` | §3 escena 3, primera subclase de `BP_StageBase` (paso 4): sobreescribe `RunStage` — acá **nacen** el HUD ProtoSoul, los 2 sensores y la elección (`BP_SoulChoice` spawneado, poll de `bChosen`, cierre por elección o cortafuegos con limpieza total). 🆕 2026-08-13 noche: **ya NO spawnea ni destruye a Alma** (Alma es del director). | 🟡 timeout verificado por log; la elección real necesita visor | [✓](BP_Stage_Hall.md) |
| 🆕 **BP_Stage_Recognizing / _Loving / _Attracting / _Surrounding** | `Core/Stages/` | Las 4 subclases restantes del paso 4 (2026-08-13 noche): Recognizing = `BP_Descent` (columnas que bajan, ilusión de ascenso) · Loving = 3 preguntas en `TextRenderActor` (cierra sola por camino real) · Attracting = spawnea `BP_AttractDirector` sobre el ecosistema de Touch colocado EN `L_Room_Attracting` · Surrounding = spawnea `BP_BrushTool` (dibujo 3D). El director ramifica por índice en `SpawnEnteringOrBase`. | 🟡 ciclo completo verificado por log; mecánicas de mano necesitan visor | [✓](BP_Stage_Subclases.md) |
| 🆕 **BP_Descent** | `Core/Rooms/` | 4 columnas fantasma (MI_Ghost) a radio 3 m que descienden a 30 cm/s — el placeholder de la ilusión de ascenso de Recognizing. `DescendSpeed`/`bDescending` editables. | 🟡 verificado por log, falta visor | [✓](BP_Stage_Subclases.md) |
| 🆕 **BP_StageBase** | `Core/Stages/` | §9.4: la clase base de las etapas — *instrucciones → ejercicio → carga → aviso al director* (placeholder por prints, paso 3 del §10). **La spawnea el director en cada sala y se autodestruye al completar**; cierra vía `ForceComplete` (camino real) y el timer del director quedó como **cortafuegos** (`CurDuration + TimeoutMargin`). En paso 4, cada etapa sobreescribe `RunStage`. | 🟡 recorrido completo verificado por log, falta visor | [✓](BP_StageBase.md) |
| 🆕🧪 **BP_SelfTest** | `Core/Debug/` | **Batería de aserciones por log** (`TEST PASS/FAIL/SKIP/SUMMARY`). Permite verificar **funcionamiento sin visor y sin humano**: `StartPIE` → `GetLogEntries("TEST ")` → `StopPIE`. 🔴 **Correr también en `bSimulate:true`** — ya encontró un bug que el PIE normal escondía. 24 aserciones. **Ya cazó 5 bugs**, dos de ellos fatales: el del streaming que dejaba la obra en negro para siempre, y una duración en 0 que la dejaba clavada en el Hall. | 🟢 24 pass / 0 fail en PIE | [✓](BP_SelfTest.md) |
| 🆕📸 **BP_TestKit** | `Core/Debug/` | **El boton MARK de las sesiones de test.** El ≡ del mando izquierdo (`IA_Menu_Toggle_Left`, que ya estaba libre en el pawn) deja de una vez: linea de log `MARK #n | etapa=i | t=s` + **captura de pantalla del instante** en `Saved/Screenshots/`. Es lo que ancla un reporte en prosa de Beltran a una ventana exacta del log. **No crea ni edita ningun IMC.** | 🟢 validado por control positivo en PIE · ⬜ falta el visor | [✓](BP_TestKit.md) |
| 🆕🤖 **BP_Robot** | `Core/Debug/` | **El usuario falso.** Mueve los `MotionControllerComponent` del pawn por `SetWorldLocation` — y como toda la obra lee las manos por los mismos dos accesores, **mover esos dos componentes es mover las manos para el juego entero**. Probado: la mano se queda donde se la pone (`dist=0.0`) y el sensor de latido se enganchó solo. `Input\|InjectInputforAction` permite sintetizar el gatillo. | 🟡 concepto probado, rutinas sin construir | [✓](BP_Robot.md) |
| **BP_DebugDirector** | `Core/Debug/` | §9.9: dispara `ForceComplete` del director (que cierra por el **mismo camino** que una finalización real, nunca teletransporta). Combo de **dos gatillos sostenidos** + modo **soak** para dejar el ciclo corriendo solo. 🆕 **HUD de debug** (2026-08-12): `TextRender` unlit pegado a la cámara con sala/tiempo/EEG/calma/ritmo, palanca `bShowHud`, refresh 0.25 s. Suelto en el persistente: se borra y listo, cero código de debug en las etapas. | 🟢 ForceComplete + HUD verificados por log · 🟡 combo y HUD sin visor | [✓](BP_DebugDirector.md) |
| 🆕 **BP_BioHub** | `Core/Signals/` | §9.3: **única fuente de verdad de calma y ritmo**. Servidor OSC propio, despacho por **comparación de string** (no `SwitchOnString`, que no se puede poblar por API), EMA de suavizado y flag de conexión con watchdog. Direcciones como variables → cambiar de dispositivo es tocar datos. Falta el binning de §9.5. | 🟡 ingesta y suavizado verificados en PIE | [✓](BP_BioHub.md) |
| **BP_SignalProvider** | `Core/Signals/` | Abstracción de la señal biométrica (respiración/latido/EEG) que consumen los stages. **Revisar antes de construir un mock del BioHub: puede que ya esté hecho.** | ⚪ por auditar | — |
| **BP_SignalProvider_Fake** | `Core/Signals/` | Mock de señal para testear sin sensor real. | ⚪ por auditar | — |
| **BP_FadeSphere** | `Core/UI/` | Esfera de fade a negro (transiciones). Compartida por los stages. | 🟢 | — |
| **BP_IntroFade** | `Core/UI/` | Fade de entrada al stage + spawnea el widget de instrucciones en su TargetPoint. | 🟢 | — |

## Stage Breath 🟢 (`Content/SoulCharger/Stages/Breath/`) — plantilla de la obra
| Blueprint | Ruta | Qué hace | Estado | Tracker |
|---|---|---|---|---|
| **BP_BreathSensor_V2** | `Stages/Breath/` | Sensor agarrable + detector de respiración + calibración/zona segura + conteo + hápticos, fusionado. Corazón del stage. `Step` frágil (no reescribir). | 🟢 | ✓ |
| **Box_Breath** | `Stages/Breath/` | La esfera visual; consumidor tonto que lee el sensor y anima escala + material emisivo + Niagara. | 🟢 | ✓ |
| **BP_Instructions** | `Stages/Breath/` | Máquina de 5 páginas (driver del widget world-space): `GotoPage/InitRefs/UpdateFade` + spawnea sensor/esfera. | 🟢 | — (ver memoria instructions-widget) |
| **WBP_BreathInstructions** | `Stages/Breath/Widget/` | El widget UMG de 5 páginas (visual). | 🟢 | — |
| **BP_BreathStageManager** | `Stages/Breath/` | Orquesta el cierre: fin de conteo → esfera a 0 → fade → reinicia. | 🟢 | ✓ |
| **BP_BreathProbe** | `Stages/Breath/Deprecated/` | Bitácora de los 26 tests que descubrieron el modelo de señal (ZigZag). Conceptualmente heredado por V2. | 🗑️ | ✓ |
| **BP_BreathSensor** | `Stages/Breath/Deprecated/` | Versión previa a la fusión en V2. | 🗑️ | — |

## Stage Heart 🟡 (`Content/SoulCharger/Stages/Heart/`)
| Blueprint | Ruta | Qué hace | Estado | Tracker |
|---|---|---|---|---|
| **BP_HeartSensor** | `Stages/Heart/` | Beat desde OSC + visualizador de zona segura (debug). 🟡 la esfera de debug queda fija en el mundo (pendiente que siga la cabeza). | 🟡 | ✓ |
| **Ball_Heart** | `Stages/Heart/` | Pulso de escala por latido (consumidor visual). | 🟡 | ✓ (`Ball.md`) |
| **BP_HeartInstructions** | `Stages/Heart/` | Driver del widget de instrucciones (duplicado de Breath). | 🟡 | — |
| **WBP_HeartInstructions** | `Stages/Heart/Widget/` | Widget UMG (duplicado de Breath). | 🟡 | — |
| **BP_HeartIntroFade** | `Stages/Heart/` | Fade + spawn del widget (duplicado). | 🟡 | — |
| **BP_HeartStageManager** | `Stages/Heart/` | Cierre del stage (duplicado de Breath). | 🟡 | — |

## Stage Touch = "Attracting" 🧩 (`Content/SoulCharger/Stages/Touch/`) — scaffold
> Plan completo: `docs/stages/touch-attracting.md`. Los stubs están vacíos, listos para construir por fase.
> 🆕 **2026-08-04 — la mecánica se rearmó** (sensores flotantes que encienden el beam, botón FINISH MELODY, sacar burbujas a mano, interp en TODOS los movimientos) y se cerró la **arquitectura de audio** (§3.b del brief). Fases nuevas **R1-R8** en el §4.b. Antes de tocar `BP_SoundBubble` o `BP_AimBeam`, leer la sección "Rearmado" de su tracker.

| Blueprint | Ruta | Qué hace (previsto) | Estado | Tracker |
|---|---|---|---|---|
| **BP_AttractDirector** | `Stages/Touch/` | Cerebro: registra `IMC_Touch`, **spawnea las burbujas por TargetPoint** (tag `BubbleSpawn`), cachea los slots por `StepIndex` y corre el playhead. Sigue **por timer**: Quartz bloqueado por un paso manual en el editor. | 🟡 | ✓ |
| **BP_SoundBubble** | `Stages/Touch/` | Burbuja sonora. Hover **push** (`HoverCount`, sirve a las 2 manos), far-grab siguiendo **el beam que la agarró**, colocación en el **slot más cercano**, **swap**, y pulso audioreactivo. La spawnea el Director, no se coloca a mano. 🆕 **2026-08-04: todo el movimiento pasó a una sola función `UpdateMove` con un único `VInterpTo`** (se acabaron los teleports) + **soltarla lejos de la mesa la manda de vuelta a casa**. Falta test en visor. | 🟢 | ✓ |
| **BP_SeqSlot** | `Stages/Touch/` | Un slot (data: `StepIndex` + `Occupant`). 5 en `L_Touch` en fila (X=55, Z=75), StepIndex 0-4 coincidiendo con el orden espacial. | 🟢 | ✓ |
| **BP_SeqTable** | `Stages/Touch/` | Mesa visual bajo los slots. **Sin construir** (los slots funcionan sin ella). | 🧩 stub | — |
| 🗺️ *(no es BP)* **Spawn de burbujas** | `L_Touch` | **20 `TargetPoint` con tag `BubbleSpawn`** (2026-08-05, eran 6). `BP_AttractDirector` en BeginPlay hace `GetAllActorsOfClassWithTag(TargetPoint, "BubbleSpawn")` y spawnea una `BP_SoundBubble` en cada uno. **Para cambiar cuántas/dónde flotan: se agregan o mueven TargetPoints, no se toca ningún Blueprint.** Repartidos en arco de ±95° alrededor del usuario sentado, radio 150-210, altura 105-185. | 🟢 | — |
| **BP_AimBeam** | `Stages/Touch/` | Láser de apuntado por mano. **2 instancias** (`bIsRight`), **attacheadas al pawn** en BeginPlay. Trace desde la pose *Aim*, hover push hacia la burbuja, y far-grab con **trigger sostenido** (`IA_Shoot_L/R` del XRFramework). 🆕 **2026-08-04: arranca APAGADO** (`bEquipped=false`) — lo enciende `BP_TouchSensor` vía `Equip()`. | 🟢 | ✓ |
| 🆕 **BP_TouchSensor** | `Stages/Touch/` | Sensor flotante que se toma **por contacto** (por distancia, sin colisión en el pawn). 2 en `L_Touch`, uno por mano (`bIsRight` instance-editable), y **cada uno solo responde a SU mano**: se attachea a ese mando, su mesh es la herramienta visible y **enciende el beam de esa mano** (`Equip`). | 🟡 falta visor | [✓](BP_TouchSensor.md) |
| 🆕 **BP_HandPointer** | `Stages/Touch/` | **Arranque limpio del puntero (2026-08-05).** Solo el **line trace por canal** que sale de la mano: se attachea al aim del pawn, traza `TraceDistance` hacia adelante y publica `TraceStart`/`TraceEnd`/`HitLocation`. Hoy se ve por **debug draw**; el visual va encima. Nace porque el Niagara de `BP_AimBeam` nunca se pudo hacer ver y su asset `LineTrace` quedó dañado. | 🟡 falta visor | [✓](BP_HandPointer.md) |
| 🆕 **BP_SaveButton** | `Stages/Touch/` | Botón **"FINISH MELODY"**. **Siempre presente**, al centro de la fila de slots y 20 cm más abajo; al llenarse los 5 slots **crece** (se "activa") en vez de aparecer, y crece un poco más al hover. Se confirma con **hold de 3 s** del gatillo apuntándolo → destruye las esferas sueltas, se esconde y dispara `OnConfirmed` (lo consume R6). | 🟢 | [✓](BP_SaveButton.md) |
| 🆕 **BP_TouchInstrPanel** | `Stages/Touch/` | **Panel de instrucciones (R7).** Copia de Calibration **con las dependencias a `Calibration/` cortadas**. Página 1 avanza al tomar un sensor; el resto con **gatillo sostenido** (radial). El **largo de `PageTexts` define cuántas páginas hay**. Al terminar se esconde y habilita `bGrabEnabled` en los beams. | 🟡 falta visor | [✓](BP_TouchInstrPanel.md) |
| **WBP_TouchInstr** | `Stages/Touch/Widget/` | El widget de las páginas (`SetScreen`/`SetInstruction`/`SetIconMaterial`/`SetTriggerProgress`). Sin dependencias a otros stages. | 🟡 | ver panel |
| 🗑️ **`Stages/Touch/Deprecated/`** | — | **Carpeta de descarte del stage (2026-08-05).** Todo lo de acá tiene **cero referencias** y no entra en la obra: `BP_TouchInstructions` + `WBP_TouchInstructions` (driver viejo duplicado de Breath, reemplazado por `BP_TouchInstrPanel`), `NS_TouchBeam` (el intento de beam abandonado) y `Ref/` (material de consulta de otro proyecto, con referencias rotas — era riesgo de cook). **No borrar sin avisar; queda como historia.** | 🗑️ | — |
| **WBP_TouchInstructions** | `Stages/Touch/Widget/` | Widget de instrucciones (fondo naranja). **Textos por definir.** | 🧩 | — |
| **SG_Melody** | `Stages/Touch/` | SaveGame de la melodía (array de 5 clip-IDs). **Falta el array.** | 🧩 stub | — |

## Stage Movement = "Surrounding" 🧩 (`Content/SoulCharger/Stages/Movement/`) — scaffold
> Plan completo: `docs/stages/movement-surrounding.md`. Sistema de dibujo 3D (cinta plana con frame por transporte paralelo, ProceduralMesh).

| Blueprint | Ruta | Qué hace | Estado | Tracker |
|---|---|---|---|---|
| **BP_DrawCanvas** | `Stages/Movement/` | Motor de geometría: dueño del `ProceduralMeshComponent` y de los datos del dibujo. `BuildTriangles`/`BeginStroke`/`AddPoint`/`EndStroke` + 3 auxiliares (`WriteRing`/`CollapseRing`/`PushMesh`) **construidos y compilando**; falta probarlo en visor. | 🟡 Fase 1 | ✓ |
| **BP_BrushTool** | `Stages/Movement/` | La herramienta: prop agarrable por proximidad que define la mano hábil, lee el gatillo y emite puntos al canvas. Auto-attach + gatillo por mano + `BeginStroke`/`AddPoint`/`EndStroke` **construidos y compilando**; ancho fijo, sin presión ni calma todavía. | 🟡 Fase 1 | ✓ |
| **BP_BrushPalette** | `Stages/Movement/` | La paleta de configuración: grilla plana de 9 celdas (3 color · 3 grosor · 3 pincel) en la mano no hábil, selección por toque. 4.1-4.3 hechas y probadas: **color funciona**; 🐛 **grosor no responde** (bug pendiente, ver tracker) — sospecha #1 = pose/alcance. | 🟡 4.3 + bug | ✓ |
| **BP_MovementIntro** | `Stages/Movement/` | Duplicado de `Core/UI/BP_IntroFade` recortado: **sólo el fade from black**. Se le sacó el spawn del `BP_Instructions` de Breath y los dos `OpenSource` de los MediaPlayer de Breath. En Fase 6 volverá a spawnear, pero el driver de páginas de Movement. | 🟢 ok | — |
| **WBP_MovementInstructions** | `Stages/Movement/Widget/` | Duplicado del widget de instrucciones de Breath, con el Border `BG` en **verde** (0.04/0.30/0.16, a 0.5). Es sólo el visual; el driver de páginas está sin hacer (Fase 6). Material propio: `Widget/Material/W_MovementInstruction`. | 🧩 scaffold | — |

## Herramienta Calibration 🟢 (`Content/SoulCharger/Calibration/`) — no es stage
| Blueprint | Ruta | Qué hace | Estado | Tracker |
|---|---|---|---|---|
| **BP_CalibDirector** | `Calibration/` | Máquina de estados de 7 segmentos guiados + persistencia por usuario. | 🟢 (falta test visor) | ✓ (cubre el sistema) |
| **BP_CalibProbe** | `Calibration/` | Sensor duplicado de V2 + `RecOn/RecOff`; calcula features para la captura. | 🟢 | ✓ (en `BP_CalibDirector.md`) |
| **SG_CalibSession** / **SG_CalibIndex** | `Calibration/` | Persistencia: un `.sav` por usuario + contador. | 🟢 | ✓ (en `BP_CalibDirector.md`) |
| **WBP_CalibInstructions** | `Calibration/Widget/` | Widget de instrucciones world-space (duplicado de Breath, Mode 1 icono+texto). Lo maneja el Director. | 🟢 (falta test visor + íconos) | ✓ (en `BP_CalibDirector.md`) |
| **BP_CalibInstrPanel** | `Calibration/` | Host del widget (WidgetComponent). El Director lo **spawnea en el TargetPoint `WidgetSpawn`** en runtime (patrón de los otros stages). | 🟢 (falta test visor) | ✓ (en `BP_CalibDirector.md`) |

## Externo
| Blueprint | Ruta | Qué hace | Estado | Tracker |
|---|---|---|---|---|
| 🆕 **BP_VRPawn_SC** | `Core/Pawn/` | El pawn VR de la obra. 🔴 **compartido**. 2026-08-28: **recentrado automático al arrancar** (`ArmRecenter` + `RecenterSeated` al final del BeginPlay) — el pawn usa `SetTrackingOrigin(Stage)` y nunca recentraba, así que al recargar el nivel el usuario no caía en el PlayerStart. | 🟡 construido y compilado; **el recentrado sólo se juzga en visor** | [✓](BP_VRPawn_SC.md) |
| **BP_OSCReceiver** | `/Game/OSC/` | Recepción de datos por OSC (única referencia funcionando del cableado del delegate). | 🟢 | ✓ |

---

## Paso 2 — el trío persistente (`Core/`, rama `core/esqueleto`)
| Blueprint | Ruta | Qué hace | Estado | Tracker |
|---|---|---|---|---|
| 🆕 **BP_ProtoSoul** | `Core/Amoeba/` | 🔴 **`bIsHUD` DEBE quedar false en el CDO** (2026-08-13): en true, CADA instancia se teletransporta por Tick a la posición del HUD frente a la cámara — las 5 candidatas se apilaban en la cara del usuario (3 días de diagnósticos errados, ver post-mortem en [[BP_SoulChoice]]). Solo `SpawnHudSoul` lo pone en true. · §5: **la ameba ES el HUD** — pulso = ritmo, agitación = calma, anillos = carga. Lazy-follow **solo en yaw** (seguir el pitch la pondría en la cara), zona muerta 10°, **pitch fijo −14° por debajo del horizonte**. `SoulMesh`/`SoulMaterial`/`VariantId` por instancia, aplicados en el **Construction Script**, para poder autorar las variantes elegibles lado a lado. 🆕 **2026-08-14: se ANCLA al slot del HUD** (`BecomeHud`, attach directo a la cámara) y trae **los 5 anillos de carga** (`Ring0..4` + `M_SoulRing`, colgados de `Body` para heredar escala y pulso) + **el viaje de la ceremonia** (`LeaveHud`/`TravelToPoint`/`ReturnToHud` con smoothstep; en la vuelta el destino se recalcula cada tick). | 🟡 verificado en PIE + aserción espacial, falta visor | [✓](BP_ProtoSoul.md) |
| 🆕 **BP_Sensor** | `Core/Sensor/` | §9.3: **uno por mano, persistente** — vive en el nivel persistente y **la etapa le dice en qué convertirse** (`SetMode(n)` indexa `ModeMeshes`/`ModeMaterials`, así agregar una etapa es agregar datos y no tocar el grafo). Refactor de `BP_TouchSensor` (§9.7), del que hereda las 5 lecciones ya probadas en visor: emparejamiento por mano, detección por **distancia al cuadrado**, `collisionEnabled=NoCollision` explícito, y verificar los valores **por instancia**. 🆕 tiene `Release()`, que el de Touch no tenía. `Mode` es un int hasta que exista el `UserDefinedEnum` (el API no los crea). 🆕 2026-08-13: mesh con **`MI_Sensor`** (blanco tenue) para que NO se confunda con una Proto Soul. | 🟡 tomar probado en visor; look nuevo sin visor | [✓](BP_Sensor.md) |

| 🆕 **BP_IntroSequence** | `Core/Flow/` | §3 escena 0: **el arranque de la obra** — negro 2 s → 3 logos de 1 s → título + subtítulo, y después llama `StartExperience()` del director (que ahora tiene `bAutoStart=false`). 🔴 **Sin UMG**: plano unlit + `TextRender`, y el fade es **por emisivo contra el fondo negro**, no por transparencia. Se planta solo frente a la mirada (sólo yaw, una vez). Faltan los botones y las imágenes. | 🟡 secuencia medida por log, falta visor | [✓](BP_IntroSequence.md) |
| 🆕 **BP_SoulChoice** | `Core/Amoeba/` | §3 escena 3: spawnea las Proto Souls candidatas **una por `TargetPoint` con tag `SoulSpawn`** (patrón de `BP_AttractDirector`: la composición se autora en el viewport, sin tocar Blueprints), detecta la elección por **proximidad de la mano** y la persiste. 💡 La elegida **no se convierte** en el HUD: el HUD **adopta** su identidad y las candidatas se destruyen. 🆕 2026-08-13: **la cadena de elección se reconstruyó** (nunca había compilado — ver post-mortem del tracker), **armado con manos despejadas**, candidatas 1.6×, arco de TPs cerrado al FOV (radio 80, ±44°). | 🟡 armado verificado por log, el toque necesita visor | [✓](BP_SoulChoice.md) |
| 🆕 **BP_SoulHUD** | `Core/UI/` | **El HUD físico del guión** (2026-08-14): actor con cadena **cámara ← cubo de cabeza ← HUD**, offset ajustable por `TP_HudAnchor` (+ preview `bEditorPreview` colgado del TP). **v2: UN `WidgetComponent` con `WBP_SoulHUD`** (diagramación de la lámina del guión): punto de sensor, slot de proto ameba, barra de carga VERTICAL (`SetCharge`), píldora con el gráfico de calma (16 barras en anillo) y pulso al BPM. Todo event-driven, TickMode Automatic, redraw 20 Hz. Lo spawnea `SeedHud`/`bDebugHudAlways`; en la obra lo hará la calibración del Hall. | 🟡 verificado por log, falta visor y arte | [✓](BP_SoulHUD.md) |
| 🆕 **WBP_SoulHUD** | `Core/UI/` | El Widget Blueprint del HUD: Canvas 800×400 con `DotConn`/`ImgSlot`/`BarCharge`(BottomToTop)/`GraphPill`+`WBar00..15`/`ImgPulse` — círculos y píldora por brush `RoundedBox HalfHeightRadius`, sin texturas. API: `SetChargeW`/`SetConnectedW`/`SetPulseW`/`PushCalmW`; arma `BarsW` en su Construct. | 🟡 compila, verificado por log vía BP_SoulHUD | ver [BP_SoulHUD](BP_SoulHUD.md) |
| 🆕 **BP_Ceremony** | `Core/Flow/` | **La ceremonia de carga** (2026-08-14, sistema 1.b del guión): UN sistema que corre al cerrar cada etapa, **antes de la puerta** — la proto ameba se desprende del slot del HUD, viaja suave al **`ChargeSpot`** de la sala (TargetPoint tagueado, dentro de cada `L_Room_*`), se le dibuja el **anillo del color de la etapa** mientras la **barra del HUD sube a n×20 %**, y vuelve al slot con sus anillos acumulados. 🔴 **El director NO la conoce**: deja el pedido en su variable `CeremonyRequest` y la ceremonia (actor colocado en `L_Persistent`) lo poll-ea y avisa con `CeremonyDone` — dependencia invertida a propósito, así el cierre funciona aunque la ceremonia no exista. VOs y SCharger son **placeholders data-driven** (array vacío = silencio + log). | 🟢 ciclo completo verificado por log (3 corridas), falta visor y clips | [✓](BP_Ceremony.md) |
| 🆕 **BP_Finale** | `Core/Flow/` | **El final entero** (2026-08-15): carga al 100 % + disolución del HUD → la ameba se desprende y viaja al alcance del brazo → **hover + gatillo** para tomarla (attach a la mano, replicando [[BP_SoulChoice]], **sin tocar ningún IMC**) → al pecho: SProtoHeart + `AppendMe` + viaje a su TargetPoint de la constelación. Lo dispara el director por `bFinaleOpen` con dependencia invertida; cortafuegos de 120 s. 🔴 El hover/grab **no es verificable en PIE** (sin visor no hay manos). | 🟡 cadena verificada por log, gesto pendiente de visor | [✓](BP_Finale.md) |
| 🆕🆕 **BP_Portrait_SC** + **WBP_Portrait_SC** | `Core/UI/` | 🔴 **El retrato que la obra le devuelve al usuario** (2026-08-27, F2 del plan de cierre). Aparece con el VO 31: corre la ameba a `TP_portrait_soul`, dispara la firma, y enciende un panel de 96×54 cm con **dos curvas por `OnPaint`+`DrawLines`** (calma y ritmo, normalizadas por el propio mínimo/máximo del usuario) una **fila de anillos de respiración** (aro = ciclo, disco interior = puntaje). Todo sigue a tres marcos que se **arrastran en el Designer**. Los datos se los pide al archivo (`CollectOnly`), así que `bFakeData` llena también el panel. Y la **melodía suena en loop con las esferas REALES** (`BP_SoundOrb_SC`), al tempo del pad. | 🟢 verificado en PIE, y desde F5 también muestra el retrato de **otro** usuario (`ShowIndex(i, alma)`); ⬜ falta la diagramación de Beltrán y el visor | [✓](BP_Portrait_SC.md) |
| 🆕🆕 **BP_Constellation_SC** | `Core/Flow/` | 🔴 **El cielo de los que pasaron antes + su recorrido** (2026-08-27, F4 y F5 del plan de cierre). Un solo actor: **construye** una ameba por entrada del archivo en 20 `TargetPoint` tagueados `ConstSpot` (`TP_const_00..19`), de a una cada `StarGap`, salteando la propia (que ya viajó sola a `soul_pick_6`); y después **deja recorrerlas**: selección **por ÁNGULO** con cualquier mano (sin colisión, sin trace), beam **visible** del sensor, y el vecino apuntado **se acerca por su propio rumbo** (`FocusPull` 400 cm, sin atril compartido) llevando **su** tarjeta pegada — el `WidgetComponent` vive dentro de [[BP_ProtoSoul_SC]], con `WidgetClass=None` hasta que se enfoca, así sólo hay **un render target vivo** — más **su** dibujo y **su** melodía. Fusiona los viejos `BP_Constellation` + `BP_ConstExplorer`. | 🟢 F4 y F5 verificadas en PIE, y el final completo corrió punta a punta (19 estrellas + la propia, 12 vecinos con datos distintos, cero `Accessed None`); ⬜ falta visor y la diagramación de los 20 puntos | [✓](BP_Constellation_SC.md) |
| 🆕🆕 **BP_SoulArchive_SC** + **SG_Portrait_SC** | `Core/Flow/` | 🔴 **El retrato persistido — la versión que se usa** (2026-08-27, F1 del plan de cierre). SaveGame de **9 arrays paralelos** (variante, color, anillos, calma CSV, ritmo CSV, respiración CSV, melodía CSV, dibujo CSV y `Stamp`) con **anillo FIFO de 20**: entra la 21, sale la más vieja. `AppendMeFromWorld()` junta solo lo que hay en el mundo (BioHub, anillo de respiración, secuenciador, canvas y alma ganadora) y `bFakeData` rellena **solo los huecos** con ficción plausible, **por la misma puerta que el dato real**. Reemplaza a `BP_SoulArchive` para la obra. | 🟢 FIFO, round-trip y recolector verificados en PIE, **y la obra ya lo llama** (F3: al compartir la ameba) | [✓](BP_SoulArchive_SC.md) |
| 🆕 **BP_SoulArchive** | `Core/Flow/` | **La persistencia multi-usuario** (2026-08-15): `SG_Constellation` (SaveGame con 6 arrays paralelos, una entrada por usuario: variante, color, anillos, promedios y melodía como string) + `AppendMe` que agrega la mía y guarda. **Round-trip de disco verificado** entre corridas. Flags de debug para sembrar y para borrar el slot. | 🟢 verificado por log, falta que la obra lo llame | [✓](BP_SoulArchive.md) |
| 🆕 **BP_Constellation** | `Core/Flow/` | Spawnea una `BP_ProtoSoul` por ameba guardada, **en TargetPoints tagueados `ConstSpot`** y con su **transform completo** (pedido de Beltrán: mover/escalar cada una a mano). Configura color, variante y **anillos** de cada usuario. Si faltan puntos lo dice en el log en vez de truncar en silencio. 12 puntos colocados de arranque. | 🟢 verificado por log | ver [BP_SoulArchive](BP_SoulArchive.md) |
| 🆕 **BP_AudioHub** | `Core/Audio/` | **El catálogo único de audio** (2026-08-15, punto 1.d del plan): `SfxMap` (Map String→SoundBase), `AmbientMap` y `VoClips` (30 por índice del guión). `PlaySfx`/`PlaySfxAt`/`PlayAmbient` (crossfade real con dos AudioComponents) y **`PlayVo(i)` que DEVUELVE LA DURACIÓN del clip** (−1 si falta) — así los tiempos del guión cuelgan del clip y no de números fijos. Catálogo vacío = todo corre igual + log de la falta. Colocado una vez en `L_Persistent`. | 🟢 verificado por log, falta cargar los archivos | [✓](BP_AudioHub.md) |
| 🆕 **BP_HapticHub** | `Core/Audio/` | Los **3 patrones hápticos** reusables sin assets (`SetHapticsByValue`): `HapticHover` (suave), `HapticSelect` (fuerte), `HapticHold` (continuo, el del umbral), con `bEnabled` para apagar todo. La mano va como `bool bRight` porque un enum **no entra por cable**, sólo como literal en el pin. ⬜ Nadie lo llama todavía — el primer candidato es el pulso de Heart. | 🟢 compila y arranca, falta cablearlo | ver [BP_AudioHub](BP_AudioHub.md) |
| 🆕 **BP_DrawLimit** | `Stages/Movement/` | **La esfera límite del dibujo de Surrounding** (2026-08-15): al invadirla, el trazo **se afina en punta y se corta** — sin lógica nueva de geometría: le pone `bTrigHeld=false` al pincel y el `TaperOut` que ya existía hace el resto. La etapa además desprende la ameba y la planta en `DrawSpot` a tamaño de dibujo. 🟡 compila; ⬜ **falta poner el anchor `DrawSpot` en `L_Room_Surrounding`** y el visor | [✓](BP_DrawLimit.md) |
| 🆕 **BP_Credits** + **WBP_Credits** | `Core/UI/` | **Los créditos del cierre** (2026-08-15): panel colgado de la cámara a **20 cm** — tiene que estar **adentro** de la esfera del fade (radio 30 cm) o el negro lo tapa. Textos editables en el widget, sin tocar Blueprint. 🟢 verificado por log; ⬜ falta visor (la distancia) y los textos reales | [✓](BP_Credits.md) |
| 🆕 **BP_ConstExplorer** | `Core/Flow/` | **Apuntar una ameba de la constelación y escuchar su melodía** (2026-08-15). Selecciona **por ÁNGULO**, no por line trace — así no hace falta darle colisión a `BP_ProtoSoul`; y como cae a la cámara si no hay mando, **se puede verificar en PIE sin visor**. 🟢 verificado por log (`EXPLORAR: ameba 0 | melodia = 1,2,3`); ⬜ falta visor y el láser visible | [✓](BP_ConstExplorer.md) |
| 🆕 **BP_ResultsPanel** + **WBP_Results** | `Core/UI/` | **El gráfico de resultados del exterior** (2026-08-15): 5 filas con el nombre de la etapa en su color y dos barras — **calma** (color de la etapa) y **ritmo** (blanca, más fina) — que se revelan **una por una**. Los promedios salen de las 6 funciones nuevas por etapa de [[BP_BioHub]]; el director ahora marca la etapa (`MarkBioStage`), que **antes no se escribía nunca**. Colocado justo debajo del `SoulHandSpot`. 🟢 cadena verificada por log; ⬜ faltan visor y una corrida completa que llene los números | [✓](BP_ResultsPanel.md) |
| 🆕 **NS_LovingField1/2/3** | `Core/VFX/` | **El arte de los 3 campos de Loving** (2026-08-15, pedido de Beltrán): duplicados de `NS_VoidDust` con **shape ESFERA** (radio 300), **CurlNoiseForce** cuya `Noise Strength` es `ScaleAndBiasFloat(User.Calm × 260 + 60)`, y **un color cada uno** (rosa/violeta/ámbar). La calma llega en vivo, medida en PIE. 🟢 compilan sin errores | [✓](NS_LovingField.md) |
| 🆕 **BP_LovingField** | `Core/VFX/` | **Un campo de luz del Acto 6** (2026-08-15). Se colocan 3 en `L_Room_Loving` y se distinguen por `FieldIndex`; [[BP_Stage_Loving]] los enciende beat a beat y les bombea la calma del EEG a 10 Hz. El sistema Niagara y los nombres de sus parámetros son **datos**: `ProbeParams` pregunta en BeginPlay si existen (`bIsValid` de `GetNiagaraVariable`) y sólo escribe los que existen, así el arte real entra sin tocar nodos. Hoy usa `NS_VoidDust`, que **no expone ningún parámetro** → la calma modula por escala del componente. | 🟢 ciclo verificado por log, arte placeholder | [✓](BP_LovingField.md) |
| 🆕 **BP_StageIntro** | `Core/UI/` | **La explicación de las etapas del Hall** (2026-08-15): actor colocado en `L_Room_Hall` con un `WidgetComponent` (`WBP_StageIntro`: 5 píldoras cápsula con los nombres en inglés y la paleta de la lámina) y un `SoulAnchor` donde nace una **[[BP_ProtoSoul]] REAL** — la ameba y los anillos son los de la obra, no dibujos de UI (decisión explícita de Beltrán). Revela una etapa cada `StepTime` pintando la píldora y abriendo el anillo del color. `IntroPlay` es la API para cuando el Hall maneje el tiempo con el VO. | 🟢 ciclo completo verificado por log + aserción espacial 0.0 cm, falta visor | [✓](BP_StageIntro.md) |
| 🆕 **WBP_StageIntro** | `Core/UI/` | El widget de la explicación: `Root`→`Pills`(VerticalBox)→`Pill0..4`(Border `RoundedBox` FixedRadius 48)→`Txt0..4`. API `InitPills`/`ResetPills`/`RevealPill(i)`; se auto-inicializa en su `Construct`. **No dibuja la ameba** — eso es un actor real al lado. | 🟢 verificado por log vía BP_StageIntro | ver [BP_StageIntro](BP_StageIntro.md) |
| 🆕 **BP_BreathPacer** | `Stages/Breath/` | **El ritmo guiado de Entering** (rework del Acto 4, 2026-08-14): un anillo world-space (`M_SoulRing` reusado) + `TextRender` que marcan **4 s inhala / 4 s aguanta / 4 s exhala, ×5** y **avanzan solos**. Es quien decide que la etapa terminó — **muere el cierre por conteo de respiraciones**; el sensor sigue moviendo el objeto pero ya no aprueba nada. Se arranca solo en BeginPlay y avisa a la etapa con `PacerFinished` (dependencia invertida, mismo motivo que la ceremonia). Posición autorable por `TP_PacerSpawn` dentro de `L_Room_Entering`. VO 9 y SCountBreath como placeholders data-driven. | 🟡 5 ciclos verificados por log (11,95 s c/u), falta visor | [✓](BP_BreathPacer.md) |
| 🆕 **BP_SoulState** | `Core/Flow/` | El **GameInstance** (§9.3): lo único que sobrevive a un cambio de nivel, y por eso el lugar donde vive la elección de Proto Soul del usuario. 🔴 **Hay que registrarlo en `DefaultEngine.ini`** (`GameInstanceClass=`) o el cast falla **en silencio** y la elección no se guarda. | 🟢 compila, sin ejercitar | [✓](BP_SoulState.md) |

## Por clasificar (revisar qué son al tocarlos)
`Core/Flow/F_Beat`, `Core/Signals/F_Signal` — prefijo `F_`. ✅ **`Core/Amoeba/F_SoulPortrait` es un `UserDefinedStruct`** (auditado 2026-08-11): el retrato de datos del usuario. `Stages/Breath/Widget/Deprecated/Widget_1` (🗑️). Auditar y mover a la tabla que corresponda cuando se trabajen.

> Assets que NO son Blueprints (materiales `M_`/`W_`, Niagara `NS_`, audio, input `IA_`/`IMC_`, fuentes) no van en este mapa — se referencian desde el tracker del BP que los usa.
